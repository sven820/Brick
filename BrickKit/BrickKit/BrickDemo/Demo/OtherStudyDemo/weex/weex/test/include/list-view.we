<template>
    <list>
        <cell repeat="{{items}}">
            <list-item title="{{name}}" src="{{src}}"></list-item>
        </cell>
    </list>
	
</template>

<script >
    require('weex-components');
	module.exports = {
		data: {
		     dir: '',
    		 items: [

                {name: '', title: '', src: ''},
            ],
		},

		created: function () {
			var bundleUrl = this.$getConfig().bundleUrl;
			var platform = this.$getConfig().env.platform;
           
		}
	}
</script>